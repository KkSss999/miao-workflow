# AGENTS.md

## 项目

- 名称：miaoworkflow
- 内部简称：**mwf**（代码、包名、CLI、分支名前缀统一用 `mwf`）
- 仓库根：`~/Codes/5k2m/miaoworkflow`

## 约定

- 骨架阶段：不要臆造技术栈。语言/运行时由用户确认后再落地（`package.json` / `pyproject.toml`）。
- 文档与注释：中文；标识符与提交信息：英文。
- 提交信息前缀 `mwf:`。
- 未经确认不要引入依赖、不要创建 CI。

## 当前状态

仅目录骨架 + git 仓库（`main` 分支，无 commit）。下一步需用户确认技术栈。
